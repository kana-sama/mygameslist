# Xenoblade note hover stability

Keep note-card and neighboring-card rectangles identical between idle, hover, and focus-within. The absolute action tray may fade in and remain clickable; feedback stays in filter/shadow only. Remove target-game hover/focus translation at desktop and narrow breakpoints. Preserve drag transform, editing, drop target, content, shelf layout, base CSS, and non-target pages. Verify at `1440×900`, `980×900`, `390×844`; temporary real-game checks only.
